import React from 'react';
import {{ camelCasedName }} from "games/{{ packageName }}";

export default {
  title: 'Games/{{ verboseName }}/Guest',
  component: {{ camelCasedName }}.Guest,
};

const Template = (args) => <{{ camelCasedName }}.Guest {...args} />;

export const MyFirstStory = Template.bind({});
MyFirstStory.args = {
  state: {},
  moves: {},
  connectionId: "xxx",
};