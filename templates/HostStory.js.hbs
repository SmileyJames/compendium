import React from 'react';
import {{ camelCasedName }} from "games/{{ packageName }}";

export default {
  title: 'Games/{{ verboseName }}/Host',
  component: {{ camelCasedName }}.Host,
};

const Template = (args) => <{{ camelCasedName }}.Host {...args} />;

export const MyFirstStory = Template.bind({});
MyFirstStory.args = {
  state: {},
  moves: {},
  connections: [],
};