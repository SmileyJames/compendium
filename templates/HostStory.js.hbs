import React from 'react';
import {{ camelCasedName }} from "@compendium/{{ packageName }}";


export default {
  title: '{{ verboseName }} Host',
  component: {{ camelCasedName }}.Host,
};

const Template = (args) => <{{ camelCasedName }}.Host {...args} />;

export const EmptyBoard = Template.bind({});
EmptyBoard.args = {
  state: { board: (new Array(9)).fill(null) },
  moves: {},
  connections: [1, 2],
};

export const Board = Template.bind({});
Board.args = {
  state: { board: (new Array(9)).fill(1) },
  moves: {},
  connections: [1, 2],
};
