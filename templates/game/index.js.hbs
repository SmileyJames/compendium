import game from "@compendium/{{ packageName }}-game";
import { Host, Guest } from "@compendium/{{ packageName }}-client";

const name = "{{ verboseName }}";

const {{ camelCasedName }} = { name, Host, Guest, game };

export default {{ camelCasedName }};
